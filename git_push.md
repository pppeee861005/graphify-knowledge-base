#以下指令是參考用，用來練習對claude code下命令
請幫我完成 `git push` 操作：

1. __`git add .`__ — 將所有未追蹤的檔案加入暫存區
2. __`git commit -m "新增 agent_os、openclaw_fb_post 等檔案"`__ — 建立新 commit
3. __`git push`__ — 推送至遠端 `origin/master`

把所有檔案推送至 GitHub 遠端倉庫：
